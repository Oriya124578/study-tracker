# Original User Request

## Initial Request — 2026-06-10T06:51:34Z

# Teamwork Project Prompt — Draft

> Status: Launched 🚀
> Goal: Craft prompt → get user approval → delegate to teamwork_preview

פיתוח פאזה 2 (עדכוני מודל נתונים וממשק משתמש) ופאזה 3 (פיצ'רים מתקדמים) לאפליקציית Calorie Life בהתבסס על מערכת העיצוב הסופית "cream v3". העיצוב אושר סופית והועלה ל-Git.

Working directory: c:\src\projects\Calorie Life
Integrity mode: benchmark

## Requirements

### R0. תאימות עיצובית מלאה (Pixel-Perfect)
- הקוד והממשק חייבים להיות **זהים לחלוטין** לעיצוב הקיים בקובץ `design-briefs/cream-INDEX.html` ול-25 מסכי ה-`cream-*.html` המקושרים אליו. 
- יש להעתיק במדויק את טיפוגרפיית ה-Editorial (גופני Instrument Serif, Fraunces, Inter), צבעי ה-cream החמים, מערכת הגריד וההתנהגות הרספונסיבית. אין לסטות מהעיצוב הזה בשום צורה.
- יש להשתמש ב-Vanilla CSS או CSS Modules טהור כדי להבטיח שליטה מוחלטת והעתקה של העיצוב (אין להשתמש ב-TailwindCSS).

### R1. פאזה 2 - ליבת הממשק ומודל הנתונים
- הגדרת שדה `courseId` כאופציונלי ב-`cl_personalTasks` והסתרת בורר הקורסים כברירת מחדל ב-UI (מופיע על-לחיצה).
- מערכת קטגוריות: הוספת collection בשם `cl_categories` (עם `name`, `color`, `icon`, `scope`). הוספת `categoryIds` למשימות/פתקים/אירועים. הצגת chips בממשק ומודאל הוספת קטגוריה. (כולל יצירת סט קטגוריות דיפולטיבי למשתמשים חדשים: לימודים, עבודה, אישי).
- סנכרון תמונת פרופיל מ-Calori (האזנה ל-`users/{uid}/profile/photoURL`), עם fallback של ראשי תיבות ורקע ירוק.
- תיקוני UI: עיגון לימין והגדלת ה-wordmark בהדר, הוספת כפתור CTA בעמוד הבית כאשר `todayItems.length === 0` ואחרי ביקור המנהל.
- הרחבת הקישור העמוק ל-Calori (`/app/day/<date>`) והצגת תמונה בארוחות.
- פיצול מסך ההגדרות ל-8 נתיבים נפרדים.

### R2. פאזה 3 - פיצ'רים מורכבים
- **סנכרון Google Calendar:** מימוש OAuth דרך Cloud Functions לאחסון מאובטח של הטוקנים. קריאת אירועים מהיומן הראשי (read-only בשלב זה) עם סימון `source: 'google'`.
- **תצוגות יומן ב-React:** יצירת 5 תצוגות (יום, 3 ימים, שבוע, חודש, לו"ז) עם Segmented control לניהול ה-state.
- **מנוע משימות חוזרות:** הוספת שדה `recurrence` ויצירת מופעים עתידיים באופן אוטומטי (עם יכולת עריכה "רק זו" או "כל ההבאות").
- **מנוע AI Manager:** פונקציית Cloud Function שרצה ב-7:00 וב-21:00 (בזמן גלובלי אחיד לכולם, למשל זמן ישראל), בודקת הקשר ומייצרת המלצות `cl_aiSuggestions`. הגבלת כמות להצעות וממשק קבלה/דחייה במסך המנהל.
- **ניהול קטגוריות:** ממשק מלא להוספה/עריכה/מחיקה של קטגוריות עם בחירת צבע ואייקון.

## Verification Resources
האימות יבוצע על ידי סוכן-שופט (Agent-as-Judge). על צוות הפיתוח להריץ בדיקות ולוודא שהקוד עומד ברובריקות המפורטות ב-Acceptance Criteria מטה.

## Acceptance Criteria

### R0. Design Consistency
- [ ] כל קומפוננטת UI שנוצרת במסגרת הפיתוח מושווית מול קובץ ה-HTML המקורי שלה (מתוך ה-25 מסכים) ומציגה תאימות זהה במבנה, טיפוגרפיה, וריווחים (שימוש ב-Vanilla CSS).

### R1. Phase 2 (UI and Data)
- [ ] שדה `courseId` מאפשר עריכה ושמירה במאגר הנתונים גם כשהוא ריק או חסר (optional).
- [ ] יצירת משתמש חדש מייצרת אוטומטית מסמכים עבור קטגוריות ברירת המחדל (לימודים, עבודה, אישי) תחת `cl_categories`.
- [ ] כפתור ה-CTA בעמוד הבית מופיע אך ורק כאשר התנאים `todayItems.length === 0` ואחרי ביקור המנהל מתקיימים, ונבדק אובייקטיבית על ידי המצב ההתחלתי.
- [ ] מסך ההגדרות מתפצל בהצלחה ל-8 ניתובים שונים ללא קריסות (Routing errors) או שגיאות 404 בהרצת הלקוח.

### R2. Phase 3 (Complex Features)
- [ ] פונקציית ה-OAuth מול גוגל שומרת טוקנים בסביבה המאובטחת של Cloud Functions בלבד, ואינה חושפת אותם לצד הלקוח.
- [ ] ה-React Calendar טוען ומציג את חמשת סוגי התצוגות השונות מבלי לאבד את ה-State של המשתמש בעת המעבר ביניהם.
- [ ] מנוע ה-Recurring Tasks מצליח לייצר מופע עתידי (Instance) למשימה באופן אוטומטי בהתבסס על אובייקט ה-`recurrence` שלה.
- [ ] פונקציית ה-AI Manager (ה-Cloud Function) תקינה ברמת הקימפול (Builds successfully), מקונפגת לרוץ פעמיים ביום באופן גלובלי (CRON schedule), ומייצרת מסמך המלצה תקין.
